import { makeStyles } from "@mui/material";

const useStyles = makeStyles((theme) => ({
    typo: {
        flexGrow: 1,
        textAlign: "center",
        color: '#323232'
    }
  }));

export default useStyles;